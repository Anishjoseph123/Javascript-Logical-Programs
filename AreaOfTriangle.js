//4. Area of a Triangle. A=(base*height)/2.

 let base = Number(prompt("Enter the value of base"));
 let height = Number(prompt("Enter the height value"));
let areaTri = (base * height) / 2;
 console.log(`The area of the given Triangle is:${areaTri}`);
